import SectionWrapper from '@/components/layouts/SectionWrapper';

const ExperiencesSection = () => {
  return (
    <SectionWrapper sectionId='experience'>ExperiencesSection</SectionWrapper>
  );
};

export default ExperiencesSection;
