import SectionWrapper from '@/components/layouts/SectionWrapper';

const ContactSection = () => {
  return <SectionWrapper sectionId='contact'>ContactSection</SectionWrapper>;
};

export default ContactSection;
